export interface TabItem {
  name: string;
}

export interface GridItem {
  name: string;
  icon: string;
}

export interface StatusCard {
  title: string;
  text: string;
  buttonText: string;
}

export interface Show {
  time: string;
  title: string;
}
